#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a: The first number to multiply
 * @b: The second number to multiply
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
int res;
res = a * b;
return ( res);
}
