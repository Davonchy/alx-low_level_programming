#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @m: number to add
 * @n: second number to be added.
 *
 * Return: result of addition.
 */
int add(int n, int m)
{
int sum;
sum = n + m;
return (sum);
}
