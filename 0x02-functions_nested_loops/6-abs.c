#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @j: the absolute int to compute.
 *
 * Return: absolute value
 */
int _abs(int j)
{
if (j >= 0)
{
return (j);
}
else
{
return (-j);
}
}
